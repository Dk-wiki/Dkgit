# Hello Dk

```
git init
git add README.md
git commit -m"first commit"
git remote add origin https://github.com/Dk-wiki/Dkgit.git
git push -u origin master
```
```
本地Git更新推送到GitHub
git add .		//要添加的文件
git commit -m"更新说明"
git push origin master
```

